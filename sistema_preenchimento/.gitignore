# Dados de runtime / estado do sistema (ficam só no servidor/G:, não no git)
__pycache__/
*.pyc

logs/*.log

consolidar_queue/*.json

exports/

programacao_frentes.xlsx

# Bases de dados de entrada (grandes, atualizadas fora do git)
base_icol/*.xlsm
base_fazendas/*.xls*

# Credenciais/config local do Supabase
supabase_config.json
supabase/.temp
supabase/.branches

# Credenciais de sessão do portal dronemgmt (ver dronemgmt_config.example.json)
dronemgmt_config.json
